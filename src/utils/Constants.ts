import { ColorResolvable } from "discord.js";

export const COLORS = {
  DEFAULT: "#FFFFFF" as ColorResolvable,
  GENERAL: "#2F3136" as ColorResolvable,
  INFO: "#B938FF" as ColorResolvable,
  ERROR: "#FF0000" as ColorResolvable,
};

export const Text = {};

Object.freeze(COLORS);
